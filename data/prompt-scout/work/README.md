# Transaction workspace

Prompt-scout and calibration workflows use this directory for incomplete
`pending-*.json` transactions. Those files are ignored and may be replaced or
removed safely; completed history lives in the parent directory's ledgers and
archives.
